create table PRs(
                    ID int not null AUTO_INCREMENT,
                    WEIGHT FLOAT,
                    EXERCISE varchar(100),
                    OWNER_NAME varchar(100),
                    PRIMARY KEY ( ID )
);