create table prs(
                    ID varchar(255) not null,
                    WEIGHT FLOAT,
                    EXERCISE varchar(100),
                    OWNER_NAME varchar(100),
                    PRIMARY KEY ( ID )
);